/* 
Handles user input for creating graphs, including vertices and edges with weights.
*/

public static class GraphCreator
{
    public static void CreateGraph(GraphBase graph)
    {
        // Implementation for reading vertices and edges (with weights) from console
    }
}